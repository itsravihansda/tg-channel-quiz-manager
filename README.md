# tg-channel-quiz-manager
A Telegram bot that lets admins upload quizzes in JSON format and post them as interactive polls to any channel. Supports both regular and quiz-type polls, stores responses in SQLite, and allows fetching results by poll ID — ideal for managing channel-based quiz content easily.
